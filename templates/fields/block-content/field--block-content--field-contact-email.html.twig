{% for item in items %}
    <div class="contact-detail-item">
        <span class="fa fa-envelope"></span>
        <span itemprop="email">
            <a href="{{ item.content['#url'].uri }}">{{ item.content['#title'] }}</a>
        </span>
    </div>
{% endfor %}